export type AudioFxName="throw"|"impactLight"|"impactHeavy"|"goal"|"uiClick";
type Preset={wave:OscillatorType;frequency:number;frequencyEnd:number;duration:number;gain:number};
const sounds:Record<AudioFxName,Preset>={throw:{wave:"sine",frequency:210,frequencyEnd:115,duration:.16,gain:.15},impactLight:{wave:"triangle",frequency:145,frequencyEnd:82,duration:.09,gain:.12},impactHeavy:{wave:"sine",frequency:82,frequencyEnd:42,duration:.24,gain:.24},goal:{wave:"sine",frequency:523.25,frequencyEnd:783.99,duration:.42,gain:.16},uiClick:{wave:"square",frequency:410,frequencyEnd:330,duration:.035,gain:.05}};
export class AudioFx{private context:AudioContext|null=null;play(name:AudioFxName,intensity=1){this.context??=new AudioContext();const c=this.context;if(c.state==="suspended")void c.resume();const p=sounds[name],now=c.currentTime,o=c.createOscillator(),g=c.createGain();o.type=p.wave;o.frequency.setValueAtTime(p.frequency,now);o.frequency.exponentialRampToValueAtTime(Math.max(1,p.frequencyEnd),now+p.duration);g.gain.setValueAtTime(Math.max(.0001,p.gain*intensity),now);g.gain.exponentialRampToValueAtTime(.0001,now+p.duration);o.connect(g).connect(c.destination);o.start(now);o.stop(now+p.duration);}dispose(){if(this.context)void this.context.close();this.context=null;}}
